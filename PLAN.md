# Codex Usage Board MVP

## 目的
Codex の残り利用率と利用枠のリセットまでの時間を、通常の macOS ウインドウで確認できるようにする。

## 方針
- SwiftUI / AppKit、macOS 14 以降。外部ライブラリなしの Swift Package とする。
- `codex app-server` の stdio JSON-RPC `account/rateLimits/read` を使い、既存の Codex ログインを利用する。
- セッション枠・週間枠、複数の利用枠、残りパーセント、リセット日時・カウントダウンを表示する。
- 60 秒ごとの自動更新、手動更新、最前面表示、接続設定、明示的なデモモードを用意する。
- 取得失敗時には最終取得値と失敗状態を区別し、未取得・欠損データを 0% と見なさない。
- API キーや認証トークンをアプリ側で読み取り・保存しない。取得だけを行い、タスク作成やリセットクレジット消費は行わない。

## 実装順序（TDD）
1. [x] 使用率の変換、複数枠、欠損値、カウントダウンの失敗テスト → 最小実装。
2. [x] JSON-RPC 接続・タイムアウト・エラーと更新状態の失敗テスト → 最小実装。
3. [x] ダッシュボード、接続設定、ウインドウの実装。
4. [x] テスト・リリースビルド、実データ接続、起動と UI の確認。
5. [x] `.app` の生成スクリプト、README、MEMORY.md の記録。

## MVP の範囲外
使用ペースからの残り作業時間予測、使用履歴、通知、ログイン項目、App Store 配布。

## 根拠
公式 App Server ドキュメント: https://developers.openai.com/ja-JP/docs/app-server
`usedPercent` は消費率、`windowDurationMins` は利用枠の長さ、`resetsAt` は Unix 秒。

## 2026-09-23 リセットクレジット表示
- [x] `rateLimitResetCredits` の利用可能数、詳細、期限の有無を失敗テストで確認する。
- [x] 詳細が取得できた分だけ期限を表示し、件数のみ・未提供・期限なしを区別する。
- [x] ボードに読み取り専用のリセットクレジット欄を追加する。
- [x] テスト、ビルド、README を更新し、結果を MEMORY.md に記録する。

## 2026-09-24 公開前の情報監査
- [x] 追跡ファイルと到達可能な Git 履歴で秘密情報・個人情報を確認する。
- [x] Git のコミットメタデータと無視対象のビルド成果物を確認する。
- [x] ソース公開とアプリ配布それぞれの対処事項を整理する。

## 2026-09-24 公開用履歴の初期化
- [x] 現在の作業ツリー、Git 参照、公開対象を確認する。
- [x] 作業記録に残る実アカウントの情報を一般化し、テストを通す。
- [x] 公開用の匿名名義で Git 履歴を初期化し、現在のソースを単一の初回コミットにする。
- [x] 履歴と作業ツリーを検証する。
