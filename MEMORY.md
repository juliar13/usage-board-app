# 作業記録

## 2026-09-20
- 空のプロジェクトから Codex 利用状況表示用の macOS アプリ MVP を開始。
- Swift 6.3.3 / Xcode の利用可否と Codex CLI 0.146.0 を確認。
- 公式 App Server ドキュメントで `account/rateLimits/read` の取得方法を確認。SwiftUI + stdio JSON-RPC を採用。
- 利用率はアカウント共通の枠として扱う。カウントダウンは利用枠リセットまでの時間。
- 実装計画を PLAN.md に記録。TDD でロジック・接続・更新状態を検証する。
- 利用枠モデルの失敗テストを先に追加し、残り率の 0–100% への補正、欠損値、複数枠優先、旧形式への互換、カウントダウンを実装。6 テストが成功。
- stdio JSON-RPC 接続、部分行・通知、応答エラー、タイムアウト、キャンセル、異常終了を疑似プロセスでテスト。JSON のスラッシュエスケープを修正して通信の 6 テストが成功。
- 更新状態のテストを先に追加。失敗時の前回値保持、認証エラー時のクリア、更新の重複抑制、接続切替時の古い応答の破棄を実装。
- ローカル App Server は ~/.codex の状態 DB を必要とするため、接続テストは通常のホスト権限で実行。既存ログインによる実データ取得テストが成功した。
- SwiftUI のダッシュボード、接続設定、最前面表示、60 秒更新、復帰時の再取得、デモ表示を実装。リリースビルドと ad-hoc 署名済み `.app` の生成に成功。
- 実画面の利用枠を Codex 本体の get_usage_limits と照合して一致。TDD で返された利用枠だけ表示するモデルへ調整し、不要な空カードを除去。
- 最終検証: 通常の自動テスト 18 件成功（実接続テスト 1 件は通常実行時スキップ）。実接続テストは明示的に別途実行して成功。
- 最終リリースビルド成功。`plutil -lint` と `codesign --verify --strict` でアプリ構造・署名を確認。
- CUA で実アプリを検証: 実データと自動更新、秒単位カウントダウン、デモの 2 枠表示、狭い幅での縦配置、無効パスのエラー表示、自動検出に戻した後の復帰、最前面トグルを確認。
- 検証後はデモを解除、Codex パスを自動検出、60 秒更新に設定して実データのウインドウを起動したままにした。最前面設定は検証前の ON に戻した。
- 成果物: `dist/Codex Usage Board.app`。生成コマンドは `./scripts/build-app.sh`。使い方・制約・検証方法を README.md に記載。

## 2026-09-23
- 利用上限の完全リセットについて、残数と各期限をボードに表示する依頼を受けた。
- インストール済み Codex の App Server スキーマで `account/rateLimits/read` 応答の `rateLimitResetCredits.availableCount` と `credits[].expiresAt` を確認。既存ログインでの読み取りも確認した。
- TDD: 残数、利用可能な明細、期限なし、明細未提供の失敗テストを追加し、モデルと UI の実装後に自動テスト 21 件が成功。
- リセット欄は読み取り専用。件数が明細数を上回る場合は不足分の期限が不明であることを明示し、未提供の情報を補完しない。
- README を更新。実接続テスト 1 件、リリースビルドと `dist/Codex Usage Board.app` の再生成に成功。

## 2026-09-24
- コミット依頼に基づき、リセット残数・有効期限表示と関連ドキュメントの変更 6 ファイルを確認。
- `swift test` を再実行し、通常テスト 20 件が成功。実接続テスト 1 件は既定どおりスキップ。
- `codex/reset-credit-display` ブランチで Conventional Commit を作成し、`main` へ fast-forward で反映する手順を採用。
- 公開前監査: 追跡ファイルと到達可能な履歴の 28 個の blob を確認。鍵・トークン・実メールアドレス・固定のホームディレクトリパスは見つからなかった。
- 旧履歴の author / committer に個人のメールアドレスとユーザー名が記録されていたため、初回公開前に履歴を初期化する方針とした。
- `.build/` と `dist/` は Git の無視対象。生成済み `.app` の実行ファイルにはビルド元の絶対パスとユーザー名が含まれる。実行ファイルのコピーを `strip -S -x` すると該当文字列がなくなることを確認。配布する場合は署名より前に strip する。
- 記録には実アカウントのリセット件数と利用枠構成が含まれていたため、公開前に一般化した。
- 公開前の初回コミットに向けて、上記の実アカウント情報を一般化した。`swift test` で通常の 20 件が成功し、実接続テスト 1 件は設定どおりスキップ。
- 旧 Git 履歴を初期化し、匿名の公開用名義で現在のソースを単一の初回コミットにする。GitHub アカウントへの実績の紐付けには、後から正しい `noreply` アドレスで履歴を作り直す必要がある。
